kind = "web"
previewPath = "/"
title = "Arc Relay Bridge"
version = "1.0.0"
id = "artifacts/arc-bridge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21219

[services.development]
run = "pnpm --filter @workspace/arc-bridge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/arc-bridge", "run", "build" ]
publicDir = "artifacts/arc-bridge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21219"
BASE_PATH = "/"
